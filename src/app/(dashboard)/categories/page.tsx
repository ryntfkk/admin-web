import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function CategoriesPage() {
  return (
    <ModulePlaceholder
      title="Kategori"
      note="Menunggu endpoint admin CRUD kategori (akan ditambahkan ke backend)."
    />
  );
}
