import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function UsersPage() {
  return (
    <ModulePlaceholder
      title="Pengguna"
      note="Suspend/unsuspend sudah tersedia di backend. List pengguna & detail butuh endpoint tambahan (akan ditambahkan)."
    />
  );
}
