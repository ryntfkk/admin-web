import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function WithdrawalsPage() {
  return (
    <ModulePlaceholder
      title="Withdrawal"
      note="Endpoint backend siap (GET/PUT /admin/withdrawals). UI daftar & proses menyusul."
    />
  );
}
