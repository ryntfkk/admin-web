import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function DisputesPage() {
  return (
    <ModulePlaceholder
      title="Sengketa"
      note="Endpoint backend siap (GET/PUT /admin/disputes). UI daftar & resolusi menyusul."
    />
  );
}
