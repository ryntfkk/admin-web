import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function AuditLogsPage() {
  return (
    <ModulePlaceholder
      title="Audit Log"
      note="Endpoint backend siap (GET /admin/audit-logs). UI tabel & filter menyusul."
    />
  );
}
