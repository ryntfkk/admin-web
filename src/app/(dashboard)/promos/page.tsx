import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function PromosPage() {
  return (
    <ModulePlaceholder
      title="Promo"
      note="Endpoint backend siap (GET/POST/PUT /admin/promos). UI daftar & form menyusul."
    />
  );
}
