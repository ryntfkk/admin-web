import ModulePlaceholder from '@/components/common/ModulePlaceholder';

export default function TransactionsPage() {
  return (
    <ModulePlaceholder
      title="Transaksi"
      note="Menunggu endpoint admin baru untuk daftar & detail seluruh transaksi (akan ditambahkan ke backend)."
    />
  );
}
